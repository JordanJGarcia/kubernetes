apiVersion: v1
kind: Pod
metadata:
  name: k8sutils
  namespace: examples
spec:
  containers:
    - name: k8sutils
      image: quay.io/iamgini/k8sutils:debian12
      command:
        - sleep
        - "infinity"
      restartPolicy: Always
